[pytest]
addopts = -q --basetemp=.tmp_pytest
norecursedirs = demo-workspaces mini-model-verify .pytest_cache artifacts .tmp .tmp_custom
markers =
    integration: integration tests that require engine_cli and subprocess execution
